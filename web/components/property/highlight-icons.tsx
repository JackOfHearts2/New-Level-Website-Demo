import { Home, BedDouble, ChefHat, Trees, Car, CalendarCheck } from "lucide-react";

export const HIGHLIGHT_ICONS: Record<string, React.ComponentType<{ className?: string }>> = {
  Home,
  BedDouble,
  ChefHat,
  Trees,
  Car,
  CalendarCheck,
};
